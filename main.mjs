import { compareTwoNames } from './compareNames.mjs';

// Call the function and update the element's content
let result = compareTwoNames("Johnny C Weymouth", "weymouth john christian");
console.log(result);